# 替换您当前的 /uploads/ 配置为以下内容：

# 上传文件访问 - 修复版本
location /uploads/ {
    alias /www/wwwroot/lndx/backend/uploads/;
    
    # 基础配置
    expires 30d;
    add_header Cache-Control "public";
    access_log off;
    
    # 确保正确的MIME类型
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }
    
    # 安全限制
    location ~ \.(php|jsp|asp|aspx|cgi|sh|bat|exe)$ {
        deny all;
    }
    
    # 防止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}

# 备用方案：如果alias不工作，使用root
# location /uploads {
#     root /www/wwwroot/lndx/backend;
#     expires 30d;
#     add_header Cache-Control "public";
# }
